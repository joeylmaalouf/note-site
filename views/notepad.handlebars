<!DOCTYPE html>
<title>Notepad: {{notepad.id}}</title>
<html>
  <head>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.3/jquery.min.js"></script>
    <script type="text/javascript" src="js/main.js"></script>
    <link rel="stylesheet" type="text/css" href="/css/pure-min.css">
    <link rel="stylesheet" type="text/css" href="/css/styles.css">
  </head>
  <body>
    <form class="centered vspaced pure-form">
      {{#unless notepad.locked}}<a class="pure-button" onclick="savetext('{{notepad.id}}');">Save Notepad</a>{{/unless}}
      {{#unless notepad.locked}}<a class="pure-button" onclick="updatepass('{{notepad.id}}');">Update Password</a>{{/unless}}
      <input id="password" type="password" placeholder="Password">
      {{#unless notepad.locked}}<a class="pure-button" onclick="setlock('{{notepad.id}}', true);">Lock Notepad</a>{{/unless}}
      {{#if notepad.locked}}<a class="pure-button" onclick="setlock('{{notepad.id}}', false);">Unlock Notepad</a>{{/if}}
      <a class="pure-button" href="/">New Notepad</a>
      <a class="pure-button" href="https://github.com/joeylmaalouf/note-site">Project Source</a>
    </form>
    {{#unless notepad.locked}}<textarea id="notepad" spellcheck="false">{{notepad.text}}</textarea>{{/unless}}
  </body>
</html>
